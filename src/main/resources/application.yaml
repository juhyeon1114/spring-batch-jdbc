spring:
  application:
    name: spring-batch-jdbc
  datasource:
    url: jdbc:postgresql://localhost:5432/batch?reWriteBatchedInserts=true
    driver-class-name: org.postgresql.Driver
    username: root
    password: password
  sql:
    init:
      mode: always
  batch:
    jdbc:
      initialize-schema: always
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
    open-in-view: false
logging:
  level:
    sql: DEBUG
